#include <stdio.h>
/**
 * main - prints out text to the stdoutput
 * @MyStr - first string
 * Return: 0
 */
int main(void)
{
	char MyStr[] = "\"Programming is like building a multilingual puzzle";

	puts(MyStr);

	return (0);

}
