#include "main.h"

/**
  * _abs - this prints the sign of a number
  * @int: variable
  * Return: (0) success
  */

int _abs(int)
{

	if (n >= 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (n * -1);
	}
}
